# source/

Imported files go here, in their real file/module layout (e.g. `source/com/example/demo/...`,
`source/src/main/...`). Only import what the investigation actually needs, not a wholesale dump.

This `README.md` is a placeholder standing in for real imported source in the demo case study —
real case studies won't have one.
